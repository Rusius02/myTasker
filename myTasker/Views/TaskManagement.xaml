<UserControl x:Class="myTasker.Views.TaskManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:myTasker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   

        <StackPanel Orientation="Vertical" Margin="10">
        <TextBox x:Name="TaskNameTextBox" Text="Nom" Margin="10"/>
            <TextBox x:Name="TaskDescriptionTextBox"  Text="Description" Margin="10"/>
            <DatePicker x:Name="TaskDueDatePicker" Margin="10"/>

        <!-- Dropdown pour les projets -->
        <ComboBox x:Name="ProjectComboBox" 
              Margin="10"
              DisplayMemberPath="Name" 
              SelectedValuePath="Id"
              Text="Sélectionnez un projet"/>

        <!-- Dropdown pour les membres -->
        <ComboBox x:Name="MemberComboBox" 
              Margin="10"
              DisplayMemberPath="Name" 
              SelectedValuePath="Id"
              Text="Sélectionnez un membre"/>
        <Button Content="Ajouter Tâche" Click="AddTaskButton_Click" Margin="10"/>
        <Button Content="Exporter les tâches" Click="ExportTasksButton_Click" Margin="10"/>


        <ListView x:Name="TasksListView" Height="200" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                        <GridViewColumn Header="Nom" DisplayMemberBinding="{Binding Name}" Width="150"/>
                        <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="200"/>
                        <GridViewColumn Header="Statut" DisplayMemberBinding="{Binding Status}" Width="100"/>
                        <GridViewColumn Header="Date Limite" DisplayMemberBinding="{Binding DueDate}" Width="150"/>
                    <GridViewColumn Header="Projet" DisplayMemberBinding="{Binding Project.Name}" Width="150"/>
                    <GridViewColumn Header="Membre" DisplayMemberBinding="{Binding AssignedMember.Name}" Width="150"/>
                    <GridViewColumn Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="❌" Background="Red" Foreground="White" 
                                                    Click="DeleteTaskButton_Click" 
                                                    Tag="{Binding}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>


</UserControl>
