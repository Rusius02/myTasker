<UserControl x:Class="myTasker.Views.ProjectManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="800" Height="600">
    <StackPanel Margin="10">
        <TextBlock Text="Create a New Project" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

        <TextBox x:Name="ProjectNameTextBox" Text="Project Name" Margin="0,5"/>
        <TextBox x:Name="ProjectDescriptionTextBox" Text="Description" Margin="0,5"/>
        <DatePicker x:Name="StartDatePicker" Margin="0,5"/>
        <DatePicker x:Name="EndDatePicker" Margin="0,5"/>
        <Button Content="Add Project" Click="AddProjectButton_Click" Margin="0,10"/>

        <ListView x:Name="ProjectsListView" Height="300" Margin="0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" />
                    <GridViewColumn Header="Start Date" DisplayMemberBinding="{Binding StartDate}" />
                    <GridViewColumn Header="End Date" DisplayMemberBinding="{Binding EndDate}" />
                    <GridViewColumn Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="❌" Background="Red" Foreground="White" 
                                        Click="DeleteProjectButton_Click" 
                                        Tag="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
